# CI scripts

Phase A check scripts for local runs and GitHub Actions.

**Full documentation:** [docs/ci.md](../../docs/ci.md) — architecture, phases, future improvements.

## Quick start

```bash
# From repo root
npm run ci
scripts/ci/ci.sh --help
```

Versions and package lists: [`manifest.env`](manifest.env).
