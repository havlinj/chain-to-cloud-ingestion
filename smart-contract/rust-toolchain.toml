[toolchain]
# Anchor 0.32 / host `cargo build` need Cargo with edition2024 (Rust >= 1.85).
# CI still pins 1.78 explicitly for `cargo test -p voting-crypto` (see smart-contract.yml).
channel = "1.85.0"
components = ["rustfmt", "clippy"]
