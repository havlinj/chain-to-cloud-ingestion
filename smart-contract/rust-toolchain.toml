[toolchain]
# Anchor / `cargo build-sbf` need stable (edition2024 in anchor-lang deps).
# CI still pins 1.78 explicitly for `cargo test -p voting-crypto` (see smart-contract.yml).
channel = "stable"
components = ["rustfmt", "clippy"]
