[package]
name = "voting-crypto"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ADR 0001/0003 cryptographic helpers (list_hash, Merkle, vote commitment)"

[dependencies]
bs58 = "0.5"
sha2 = "0.10"
sha3 = "0.10"

[dev-dependencies]
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
