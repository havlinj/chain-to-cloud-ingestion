# AWS Terraform Modules

Reusable modules for AWS resources. Add modules here when implementing the pipeline (e.g. lambda, sqs, dynamodb). Each module should have its own `main.tf`, `variables.tf`, `outputs.tf`, and follow the naming convention and IAM rules in `.cursor/rules/terraform_style.mdc`.
