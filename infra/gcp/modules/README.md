# GCP Terraform Modules

Reusable modules for GCP resources. Add modules here when implementing the analytics pipeline (e.g. pubsub, bigquery, cloudrun). Each module should have its own `main.tf`, `variables.tf`, `outputs.tf`, and follow the naming and IAM rules in `.cursor/rules/terraform_style.mdc`.
